apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: pragnya
spec:
  selector:
    app: dotnet
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5002