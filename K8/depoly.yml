apiVersion: apps/v1
kind: Deployment
metadata:
  name: dotnet
  namespace: pragnya
  labels:
    app: dotnet
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dotnet
  template:
    metadata:
      labels:
        app: dotnet
    spec:
      containers:
      - name: dotnet
        image: pragnya.azurecr.io/dotnet:1.0.1
        ports:
        - containerPort: 5000
      imagePullSecrets:
        - name: secret
